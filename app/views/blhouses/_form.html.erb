<%= form_for(blhouse) do |f| %>
  <% if blhouse.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(blhouse.errors.count, "error") %> prohibited this blhouse from being saved:</h2>

      <ul>
      <% blhouse.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
